'use client';

import { Toaster } from 'react-hot-toast';

/**
 * Toast notification component wrapper.
 * Initializes the React Hot Toast library.
 */
const Toast = () => <Toaster />;

export default Toast;
